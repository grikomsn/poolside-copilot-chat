---
"poolside-copilot-chat": minor
---

Add native API-key provider entries so multiple Poolside accounts can be configured independently in Manage Language Models. Legacy Secret Storage commands remain supported, and model discovery is isolated per provider entry. Align request timeout, stream-idle timeout, catalog-cache, and advertised output-limit settings with the sibling providers.
